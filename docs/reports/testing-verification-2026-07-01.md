# Testing Verification — 2026-07-01/02

Executed as part of [`docs/tasks/branch-04-testing-verification.md`](../tasks/branch-04-testing-verification.md),
after the docs-sync + widgets-barrel work landed. Run against the repo at commit `3a296e0` +
this session's changes (widgets barrel, `scripts/analyze-bundle.js`, docs fixes).

---

## Unit Tests (Vitest)

```
bun run test:unit:coverage
```

- **7 test files, 78 tests — all passed.**
- Coverage (scope: `src/shared/lib/i18n/**`, `src/shared/config/i18n/**`, per `vitest.config.ts`):

  | Metric | Result | Threshold |
  |---|---|---|
  | Statements | 100% (40/40) | 90% |
  | Branches | 96% (24/25) | 90% |
  | Functions | 100% (11/11) | 90% |
  | Lines | 100% (38/38) | 90% |

  All above the 90% gate. The one uncovered branch is `translations.ts:27`.

## Build Status

```
bun run build
```

- **Passed.** `astro check`: 201 files, **0 errors, 0 warnings, 8 hints** — all 8 hints are
  `ts(6133)` unused-variable warnings inside `coverage/lcov-report/prettify.js` (a third-party
  file generated by a prior `test:unit:coverage` run, gitignored, unrelated to project source).
- 35 pages built in ~1.6s. No build-time regressions from the `src/widgets/index.ts` barrel
  export or the `@widgets` bare-import path added to `tsconfig.json`.
- `bun run lint` and `bun run format:check`: clean, zero issues.
- `bun run bundle:analyze` (new `scripts/analyze-bundle.js`): runs successfully against the
  build output, total `dist/` size **4.3MB** — within the 5MB CI threshold.

## Lighthouse / Performance

```
bun run lighthouse:collect && bun run lighthouse:assert
```

Ran successfully against the production build (`npx serve ./dist`), 3 runs per page, 6 pages —
**all assertions in `.lighthouserc.json` passed.** Average scores:

| Page | Performance | Accessibility | Best Practices | SEO |
|---|:---:|:---:|:---:|:---:|
| `/` | 99 | 98 | 96 | 100 |
| `/about` | 99 | 100 | 96 | 100 |
| `/projects` | 99 | 100 | 96 | 100 |
| `/es/` | 99 | 97 | 96 | 100 |
| `/es/about` | 99 | 100 | 96 | 100 |
| `/es/projects` | 99 | 100 | 96 | 100 |

All comfortably clear the required thresholds (Performance ≥90, Accessibility ≥95, Best
Practices ≥90, SEO ≥90).

## E2E Tests (Playwright)

```
bun run test
```

**163 passed, 127 failed** across 5 projects (chromium, firefox, webkit, Mobile Chrome, Mobile
Safari). Breaking down the failures:

| Project | Failures | Root cause |
|---|:---:|---|
| **chromium** | 0 | — clean run |
| **firefox** | 2 | Visual-regression snapshot mismatch (`projects section`, `about-me section` in `visual.spec.ts`) |
| **webkit** | 58 (all tests) | Sandbox is missing system libraries for WebKit (`libicu74`, `libjpeg-turbo8`, `libwoff1`, `gstreamer1.0-libav`) — `browserType.launch` fails before any test logic runs |
| **Mobile Chrome** | 9 (all `visual.spec.ts`) | Visual-regression snapshot mismatch (same 9 sections as firefox, plus hero/header/footer) |
| **Mobile Safari** | 58 (all tests) | Same missing-system-deps failure as webkit (Mobile Safari uses the WebKit engine) |

**Assessment**: this is a sandbox/environment limitation, not a functional regression —
`chromium` (the primary/reference project) is 100% clean, and every non-visual test (homepage,
pages, a11y, navigation) passes on every project that can actually launch its browser. The
WebKit/Mobile Safari failures are entirely due to missing OS-level dependencies in this sandbox;
`.github/workflows/ci.yml` already runs `playwright install --with-deps` before the `playwright`
job, so this does not affect CI. The firefox/Mobile Chrome visual-snapshot mismatches are
consistent with font-rendering/anti-aliasing differences between this sandbox and whatever
machine originally captured the baseline PNGs under `tests/e2e/visual.spec.ts-snapshots/` — not
a code defect, since the same components render correctly per chromium, Lighthouse, and the
unit-test suite.

`playwright.config.ts`'s 5 projects (Chromium, Firefox, WebKit, Mobile Chrome/Pixel 5, Mobile
Safari/iPhone 12) still match the project's stated device coverage in `docs/TESTING.md` — no
config drift found.

## Recommendations

1. **Run the E2E suite in CI or a fully-provisioned dev container** (see
   `.devcontainer/`) to get a clean read on WebKit — this sandbox cannot install system
   packages, so it isn't representative of CI/production runs.
2. **If the firefox/Mobile Chrome visual failures reproduce in CI**, regenerate baselines there
   with `bun run test:snapshots` (not from this sandbox, to avoid baking in
   environment-specific rendering) and review the diffs before committing.
3. No action needed on unit tests, build, lint, format, or Lighthouse — all green with margin.
