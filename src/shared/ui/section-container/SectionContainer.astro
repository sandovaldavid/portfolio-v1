---
interface Props {
	class?: string;
	id?: string;
}

const { class: className, id } = Astro.props;
---

<section
	{id}
	class:list={[
		'w-full container',
		'flex flex-col justify-center items-center',
		'scroll-mt-12',
		className,
	]}
>
	<slot />
</section>
