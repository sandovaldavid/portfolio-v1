---
interface Props {
	class?: string;
	id?: string;
}

const { class: className, id } = Astro.props;
---

<section
	{id}
	class:list={[
		'w-full max-w-5xl px-4 md:px-6 mx-auto',
		'flex flex-col justify-center',
		'scroll-mt-[4.5rem]',
		className,
	]}
>
	<slot />
</section>
