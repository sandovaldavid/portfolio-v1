{
	"installCommand": "bun install --frozen-lockfile",
	"buildCommand": "bun run build",
	"git": {
		"deploymentEnabled": false
	},
	"rewrites": [
		{
			"source": "/links",
			"destination": "https://www.linkdevs.social"
		}
	]
}
